{
  "mcpServers": {
    "phantom-connect-sdk": {
      "type": "sse",
      "url": "https://docs.phantom.com/mcp"
    },
    "phantom-mcp": {
      "command": "npx",
      "args": ["-y", "@phantom/mcp-server"],
      "env": {
        "PHANTOM_APP_ID": "${PHANTOM_APP_ID}"
      }
    }
  }
}
